export const publicRoutes: string[] = ["/"];

export const authRoutes: string[] = ["/login"];

export const apiAuthPrefix: string = "/api/auth/callback";

export const DEFAULT_LOGIN_REDIRECT = "/manage-guilds";
